<% @page.sections&.each_with_index do |section, index|
  next if section.nil? %>
  <%= render_section(section, index) %>
<% end %>
<% if @page.floatingCta.present? %>
  <%= render_button(@page.floatingCta, class: 'btn-primary floating-cta d-block d-sm-none') %>
<% end %>
